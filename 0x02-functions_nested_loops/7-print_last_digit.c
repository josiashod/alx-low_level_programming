#include "main.h"

/**
 * print_last_digit - prints the last digit of a number.
 * @n: number whose last digit should be print
 *
 * Return: the last digit of @n.
 */
int print_last_digit(int n)
{
	return (n % 10);
}
